import { Dimensions } from "react-native";

const deviceScreenWidth = Dimensions.get("window").width;
const deviceScreenHeight = Dimensions.get("window").height;

export { deviceScreenWidth, deviceScreenHeight };
