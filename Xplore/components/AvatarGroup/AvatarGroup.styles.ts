import { StyleSheet } from "react-native";

export default StyleSheet.create({
  groupAvatarContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  singleAvatar: {
    marginLeft: -15,
  },
});
