import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 10,
    borderWidth: 1,
    overflow: "hidden",
  },
  textStyle: {
    textAlign: "center",
  },
});
