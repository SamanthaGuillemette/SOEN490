import { StyleSheet } from "react-native";

export default StyleSheet.create({
  listContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  user: {
    marginRight: 40,
  },
  checkCircle: {
    position: "absolute",
    top: 30,
    left: 200,
  },
});
