import { StyleSheet } from "react-native";

export default StyleSheet.create({
  userRankContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  userRank: {
    paddingHorizontal: 10,
    fontWeight: "bold",
  },
});
