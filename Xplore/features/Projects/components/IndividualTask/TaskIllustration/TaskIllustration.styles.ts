import { StyleSheet } from "react-native";

export default StyleSheet.create({
  taskImage: {
    width: "100%",
    resizeMode: "contain",
  },
  main: {
    marginLeft: 10,
    marginRight: 10,
  },
});
