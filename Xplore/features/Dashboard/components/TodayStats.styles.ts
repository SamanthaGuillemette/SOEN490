import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginBottom: 30,
  },
  todayTitle: {
    marginBottom: 6,
  },
  todayStats: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
});
