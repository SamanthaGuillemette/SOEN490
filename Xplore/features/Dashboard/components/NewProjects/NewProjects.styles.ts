import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginBottom: 30,
  },
  subTitleContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 10,
  },
});
